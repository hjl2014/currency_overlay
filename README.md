# currency_overlay

This repository demonstrates a simple currency overlay program for an international equity portfolio. The `currency_overlay.py` script downloads historical prices, calculates returns, and optimizes hedge ratios along an efficient frontier.

## Requirements
All dependencies are listed in `requirements.txt`.

## Usage
Run the script with Python to generate the overlay analysis:

```bash
python currency_overlay.py
```
